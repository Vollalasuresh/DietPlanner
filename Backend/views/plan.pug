extends base
block content
    //- link(rel="stylesheet", href="index.css" type='text/css')
    //- a(href="logout" class='logout') Logout
    h2 #{dps.day}'s plan
    ol Breakfast
        each i in bfs
            li #{i}
    ol lunch
        each i in lns
            li #{i}
    ol Dinner
        each i in dns
            li #{i}
